export class CartInfo {
  id: number;
  nearExpDateWarnCount: number;
  expWarnCount: number;
  discrepancy: boolean;
}

export class Cart {
  id: number;
  tagAddress: string;
  lastUpdate: string;
  nearExpDateWarnCount: number;
  expWarnCount: number;
  discrepancy: boolean;
}
